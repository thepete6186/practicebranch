rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Dev / classroom use only: no authentication wired yet.
    // Replace with signed-in user checks before any production deployment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
